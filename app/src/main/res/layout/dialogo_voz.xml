<?xml version="1.0" encoding="utf-8"?>
<!--
  Pantalla de voz al tocar el micrófono o entrar directamente desde una detección.
  Presenta el orbe fluido reactivo estilo Gemini Live y las partículas orbitantes.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/verde_oscuro"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="28dp">

    <TextView
        android:id="@+id/vozEstado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/pildora_estado"
        android:paddingHorizontal="18dp"
        android:paddingVertical="8dp"
        android:text="@string/chat_escuchando"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:textStyle="bold" />

    <com.example.rumiologia.asistente.CirculoVozView
        android:id="@+id/circuloVoz"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_marginVertical="28dp" />

    <TextView
        android:id="@+id/vozTexto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:minHeight="48dp"
        android:textColor="@color/chat_subtitulo"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/vozCancelar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:paddingHorizontal="24dp"
        android:paddingVertical="12dp"
        android:text="@string/voz_cancelar"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        android:textStyle="bold" />

</LinearLayout>
